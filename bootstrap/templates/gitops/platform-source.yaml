apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: reefops-platform
  namespace: flux-system
spec:
  interval: 10m
  ref:
    commit: PLATFORM_COMMIT
  url: https://github.com/OWNER/reefops-platform.git
